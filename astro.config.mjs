// @ts-check
import { defineConfig } from 'astro/config';

// https://astro.build/config
export default defineConfig({
	site: 'https://hoarderic-cell.github.io',
	base: '/hoard-technology-advisors',
});
